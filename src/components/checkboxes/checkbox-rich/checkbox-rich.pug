mixin checkbox-rich(id, title, description)
  p.checkbox-rich
    input.checkbox-rich__checkbox.visually-hidden(id=id type="checkbox")&attributes(attributes)
    label.checkbox-rich__label(for= id)= title
    span.checkbox-rich__description= description