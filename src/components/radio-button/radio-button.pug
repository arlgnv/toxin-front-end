mixin radio-button({id = "set id!", title = "set title!"})
  p.radio-button
    input.radio-button__field.visually-hidden(id= id type="radio")&attributes(attributes)
    label.radio-button__label(for= id)= title