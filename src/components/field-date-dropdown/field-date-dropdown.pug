mixin c-field-date-dropdown(classes, id, title, showLabel)
    if (showLabel == true)
        label(class= classes + "__label c-field-date-dropdown__label" for= id)= title
    else
        label(class= classes + "__label visually-hidden c-field-date-dropdown__label" for= id)= title
    input(class=classes + "__field c-field-date-dropdown__field" id= id)&attributes(attributes)