mixin range({id = "set id!", title = "set title!", description = "set description!"})
  span.range.js-range
    span.range__group
      label.range__label(for= id)= title
      span.range__price
    input.range__field(id= id type="text")&attributes(attributes)
    span.range__description= description