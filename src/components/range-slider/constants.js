const DEFAULT_RANGE_SLIDER_PARAMETERS = {
  skin: 'custom',
  type: 'double',
  min: 0,
  max: 15000,
  hide_min_max: true,
  from: 5000,
  to: 10000,
  hide_from_to: true,
};

export default DEFAULT_RANGE_SLIDER_PARAMETERS;
